public class OperadoresDeAtribuicao {
    public static void main(String[] args) {
    // = / += / -= / *= / /= / %=
        int salario = 2500;
        salario = salario % 2;
        System.out.println(salario);
    }
}
